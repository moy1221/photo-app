<%# categories = contentful.entries(content_type: ENV['CONTENTFUL_COURSE_ID']) %>
<%# categories.each do |entry| %>
  <!--<div class="contentful_product">
    <h1><%#= entry.fields[:title] %></h1>
    <p><%#= entry.fields[:description] %></p>
    <img src="<%#= entry.image.image_url %>" height="300" width="400" />
    <hr>
  </div>-->
<%# end %>
<h1><%= @entry.title %></h1>
<p><%= @entry.description %></p>
<%= link_to 'Edit', contentful_edit_path, class: "btn btn-primary" %>